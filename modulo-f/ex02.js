function parimp(n) {
    if (n%2 == 0) {
        return 'Par!'
    } else {
        return 'Ímpar!'
    }
}

/*
let res = (parimp(4))       //Modo completo
console.log (res) */

console.log(parimp(4))     //Modo simplificado